# Dependencies
node_modules/

# Next.js
.next/
out/

# ZK build artifacts (large binary files — generated locally)
circuits/build/
circuits/*.ptau
circuits/*.zkey

# Public ZK files (wasm, zkey, verification key) are committed so the
# browser can load them statically — this circuit's proving key is small
# enough (~260KB) that there's no need to exclude it.

# Env
.env
.env.local
.env.*.local

# OS
.DS_Store
.vercel
